start on startup
console log
env NAME=bitcoind

script
    echo $$ > /var/run/$NAME.pid
    sudo -Eu ubuntu /usr/bin/bitcoind -datadir=/btc
end script

post-stop script
   rm -f /var/run/$NAME.pid
end script
