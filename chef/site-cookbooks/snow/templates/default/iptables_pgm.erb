# Snow api Locked Down
# Allow connection from the workers
<% @env['cryptos'].each do |crypto| -%>
-A INPUT -p tcp -s <%= @env[crypto]["host"] %> --dport 5432 -j ACCEPT
-A OUTPUT -p tcp -d <%= @env[crypto]["host"] %> --sport 5432 -j ACCEPT
<% end %>