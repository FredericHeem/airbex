{
    "type": "object",
    "properties": {
        "number": {
            "type": "string",
            "required": true,
            "pattern": "^[0-9-\\.,\\(\\) ]{1,13}$"
        },
        "country": {
            "type": "string",
            "required": true,
            "pattern": "^[A-Z]{2}$"
        }
    }
}
