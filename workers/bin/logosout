var config = require('konfu')
, BitcoinOut = require('../lib/bitcoinout')
, ep = {
    host: config.lgs_host,
    port: config.lgs_port,
    user: config.lgs_user,
    pass: config.lgs_pass,
    ssl: config.lgs_ssl || false
}
, dbClient = require('../lib/db')(config.pg_url, config.pg_native)
new BitcoinOut('lgs', ep, dbClient)
