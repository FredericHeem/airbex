check process <%= @service %>
    with pidfile /var/run/<%= @service %>.pid
    start program = "/sbin/start <%= @service %>"
    stop program = "/sbin/start <%= @service %>"
    if 3 restarts within 5 cycles then restart



